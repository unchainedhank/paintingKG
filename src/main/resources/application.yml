#spring.data.neo4j.uri=bolt://localhost
#spring.data.neo4j.username=neo4j
#spring.data.neo4j.password=blackarrow1
#
spring:
  data:
    neo4j:
      uri: bolt://localhost
      username: neo4j
      password: blackarrow1
    elasticsearch:
      client:
        reactive:
          username:
          password:
  thymeleaf:
    cache: false

server:
  port: 8080

#classpath表示启用的是哪个配置文件
logging:
  config: classpath:log4j2-spring.xml
